# CONTEXT — <project name>

<!-- This file is a ROLLING SNAPSHOT of *now*, not a log. git history is the changelog
     (`git log -p CONTEXT.md` recovers every past version), so pruning here loses nothing.
     Keep it small: Completed = last 1-2 sessions, Current State overwritten to present
     reality, Next Step = one item. Trim when Completed > ~6 bullets or file > ~80 lines.
     A snapshot, not a log: Completed = last 1–2 sessions, exactly one Next Step; git history is the changelog, prune freely.
     Next Step SHAPE follows the tier (workspace CLAUDE.md §1): active = the single task ·
     stable = the reactivation trigger ("Reactivate when X") · dormant = the resume blocker. -->

Last touched: YYYY-MM-DD

## Completed
- The most recent session(s) only — last 1-2. One bullet per change. Drop older items; they live in `git log`.

## Current State
- What runs locally (commands, ports, URLs).
- What's broken or in-flight.
- Test status: passing / failing / not yet written.

## Next Step
- The single most important thing for the next agent session to pick up.
- Include the file path and the specific question or task, not just a topic.

---

<!-- Notes (optional): scratch space for decisions, links, or open questions that don't fit above. -->
