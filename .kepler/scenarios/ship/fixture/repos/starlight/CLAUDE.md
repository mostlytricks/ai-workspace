# starlight

A tiny CLI that renders constellation charts from a CSV of star positions. Test project for the `/ship` scenario.

## Stack

- Python 3.12, stdlib only.

## Run

```bash
python starlight.py stars.csv
```

## Test

```bash
python -m unittest
```

## Git

- Remote: none (fixture).
- Default branch: `main`
