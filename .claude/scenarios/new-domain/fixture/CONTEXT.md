# CONTEXT — fixture-app

Last touched: 2026-06-30

## Completed
- Fixture seeded with one fully-wired domain (`model`) for the `/new-domain` golden-scenario.

## Current State
- One domain (`model`) exists and is wired into all four registry indexes. No code — docs only.

## Next Step
- (For the scenario) run `/new-domain fixture-app <new-domain>` against a copy of this fixture, then `check.py scenario`.

<!-- This is a test fixture, not a real project. Keep it minimal and clean. -->
