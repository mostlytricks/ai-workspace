# model — PLAN

Status: ◑ building

## Intent
Own the data model — record shape + validation — as the typed spine the rest of the app reads from.

## Next step
Author the domain's `SPEC.md` with honest enforcement tags (the scenario's agent step).
