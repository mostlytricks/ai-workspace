# INTAKE — ai-workspace (gravity) — 2026-07-13

Batch: 2 items from live-use field reports (the first dogfood batch — gravity
running `/intake` on itself).
Status: ✓ closed

## Items

### I1 — check.py selftest red on Linux
- **Reporter · date:** Fable remote session · 2026-07-12
- **Observed (verbatim):** "SELFTEST FAILED — [FAIL] tree is dirty - preflight must pass before anchoring … leftover untracked/modified paths after reset: ?? __pycache__/ ?? tests/__pycache__/"
- **Expected:** selftest green on every OS — a known-red wall trains agents to ignore red
- **Repro:** run `python3 .claude/scenarios/check.py selftest` on Linux (bytecode-writing Python) — the patch-loop replay's gate runs write `__pycache__/`, dirtying the preflight and rollback tree-clean proofs
- **Env:** gravity v2.5.0 · Linux container · Python 3.11 · replay repo seeded with `.gitignore` = `state/` only
- **Evidence:** commit `abe2b4d` message (first recorded), selftest console output in-session
- **Triage:** real: yes (reproduced) · kind: bug · domain: scenarios harness · severity: S2
- **→** fixed 2026-07-13 in this batch — replay `.gitignore` now seeds `__pycache__/` too (`check.py` patch-loop half); regression wall: selftest itself runs the gate in a bytecode-writing env on every invocation. See CHANGELOG `[Unreleased]`.

### I2 — /intake over-triggers on plain code-analysis tasks
- **Reporter · date:** workspace owner · 2026-07-13
- **Observed (verbatim):** "when i apply it, to analyze codes and domain, my coding agent firstly go to intake, not directly analyze. Is it.. intended one? Actually works well, but i thought it was for issue and error claims management."
- **Expected:** `/intake` self-selected only when the input is a batch of reported claims; plain code/domain analysis reads the code directly
- **Repro:** give a coding agent a code/domain analysis task with issue-adjacent wording; it selects `/intake` first (command is deliberately model-invocable; description carried no boundary)
- **Env:** gravity v2.5.0 · Claude Opus 4.8 coding agent · user workspace
- **Evidence:** user field report in-session (verbatim above); confirmed against the v2.5.0 description text
- **Triage:** real: yes (mechanism confirmed) · kind: doc-drift (the fence was missing, the ritual itself was right) · domain: commands · severity: S3
- **→** fixed in **v2.5.1** — trigger fence in the frontmatter description + "When NOT to reach for this" section (CHANGELOG `[2.5.1]`). No slice needed: wording-only.

## Root causes (dedupe)

| Cause | Items | Slice PLAN | Queue lane |
|---|---|---|---|
| replay repo ignores too little (tool-made bytecode dirt) | I1 | fixed in-batch (check.py) — no standing PLAN | — |
| model-invocable command without a trigger fence | I2 | fixed in v2.5.1 — no standing PLAN | — |

## Batch close

- [x] Every item's `→` line points somewhere (both fixed, citations above).
- [x] Dupes collapsed (none — two distinct causes).
- [x] Queue rows: none needed — both causes closed within their fix releases.
- [x] Verified: `python .claude/scenarios/check.py intake --project .` — OK.
